/*
Functions -> which takes input and gives output
=> Some pre built
    1. console.log() -> takes some data as a input and the displays them in the output.
                     -> we can also pass multiple values
                     -> console.log("hello",a," ",b," dekha");
*/
